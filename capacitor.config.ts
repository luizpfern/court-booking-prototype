import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'court-booking-prototype',
  webDir: 'www'
};

export default config;
